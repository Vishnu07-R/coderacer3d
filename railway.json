{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "cd server && npm install && npm run build && npx prisma generate && npx prisma migrate deploy || true"
  },
  "deploy": {
    "startCommand": "cd server && npm start",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  },
  "env": {
    "PORT": "3001",
    "NODE_ENV": "production"
  }
}
